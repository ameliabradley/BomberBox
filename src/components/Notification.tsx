import * as React from "react";
const styles = require("./Notification.css");

const Notification = () => (
  <div className={styles.displayTextCenter}>
    <span className={styles.displayText} />
  </div>
);

export default Notification;
